#include <iostream>
#include <thread>

using namespace std;
/*
void func(int n)
{
    cout<<"thread " << n << " is running" <<endl;
}

int main(int argc, char *argv[])
{
    thread t1(func, 1);
    t1.join();

    thread t2(func, 2);
    thread t3(func, 3);
    thread t4(func, 4);
    t2.join();
    t3.join();
    t4.join();
    return 0;
}
*/
